import * as React from "react"

const HomePage=()=>{
  return(
   <div>
    <h2>
      Home page.
    </h2>
   </div>
  )
}

export default HomePage

export const Head = () => <title>Home Page</title>
